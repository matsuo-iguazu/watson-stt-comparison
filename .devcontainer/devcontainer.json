{
  "name": "Watson STT (Python)",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "eamodio.gitlens"
      ]
    }
  },
  "settings": {
    "terminal.integrated.shell.linux": "/bin/bash"
  },
  "forwardPorts": [],
  "postCreateCommand": "python -m pip install --upgrade pip && pip install -r requirements.txt"
}
